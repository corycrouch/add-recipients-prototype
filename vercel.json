{
  "installCommand": "rm -f .npmrc \"$HOME/.npmrc\" && printf 'registry=https://registry.npmjs.org/\\n' > .npmrc-public && env -i HOME=\"$HOME\" PATH=\"$PATH\" USER=\"$USER\" npm install --userconfig=.npmrc-public"
}
